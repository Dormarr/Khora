//This is currently unused, but I'd like to implement it in the same way as
//the natural feature enum. But because the biome system uses lookup by name,
//I would have to tinker to lookup by enum and that's not urgent enough to
//justify right now. I'll do it at some point though.

public enum BiomeEnum
{
    borealForest,
    desert,
    forest,
    glacial,
    plains,
    rainforest,
    shrubland,
    snowyForest,
    swamp,
    tundra,
    wetlands,
    taiga

}
